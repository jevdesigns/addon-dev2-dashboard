# Example environment variables for local development with Vite
# Set these in a local `.env` file (do NOT commit it).
# The app will use these values when opening Settings if no saved settings exist.
VITE_HA_WS=/api/websocket
VITE_HA_TOKEN=PASTE_YOUR_TEST_TOKEN_HERE
